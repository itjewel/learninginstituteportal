import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['scontent.fdac24-1.fna.fbcdn.net','ekushlanguage.com','images.unsplash.com','scontent.fdac24-1.fna.fbcdn.net'], // Add the domain here
  },
  /* config options here */
};

export default nextConfig;
